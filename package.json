{
  "name": "instagram-unfollow-radar",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "check": "node --test && find src -name '*.js' -exec node --check {} \\;",
    "package": "python3 scripts/build_extension.py",
    "test": "node --test"
  }
}
